#include "minishell.h"

// if (ft_strcmp(cmd, ".") == 0)
	// {
	// 	fprintf(stderr, "minishell: %s: filename argument required\n", cmd);
	// 	return (2);
	// }
	// if (ft_strcmp(cmd, "..") == 0)
	// {
	// 	fprintf(stderr, "minishell: %s: command not found\n", cmd);
	// 	return (127);
	// }